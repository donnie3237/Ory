# ... เหมือนเดิม แต่เพิ่ม volumeMount
volumeMounts:
  - name: config
    mountPath: /etc/config
    readOnly: true
volumes:
  - name: config
    projected:
      sources:
        - configMap:
            name: kratos-config
        - configMap:
            name: kratos-identity-schema
